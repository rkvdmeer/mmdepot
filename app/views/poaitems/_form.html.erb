<%= form_for(@poaitem) do |f| %>
  <% if @poaitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poaitem.errors.count, "error") %> prohibited this poaitem from being saved:</h2>

      <ul>
      <% @poaitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.number_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
